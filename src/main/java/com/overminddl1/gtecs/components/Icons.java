package com.overminddl1.gtecs.components;

import com.artemis.Component;
import com.artemis.annotations.Transient;

@Transient
public class Icons extends Component {
	// List icons in here to preload them so everything can access them
	public String[] icons;
}
